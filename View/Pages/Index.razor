@page "/"

// Erstellen Sie ein Chart mit einem Typen ihrer Wahl um die Daten der Stock Klasse abbilden zu können
// Die Dokumentation finden Sie unter https://apexcharts.github.io/Blazor-ApexCharts/

// Weiters müssen Sie sich die Daten über die vordefinierte Schnittstelle (TiingoApi) holen

// Zeigen Sie ein animiertes Icon an, während die Daten geladen werden
// Die Animationsklasse sind unter https://animate.style/ zu finden

// Das Icon soll vorher mit dem Programm SvgPathEditor erstellt werden und soll in den Maßen 16x16 sein

// sollte das Icon nicht ausgegeben werden, dann benutzen Sie die Funktion Thread.Sleep(5000) um eine Verzögerung zu simulieren

// sollte noch Zeit vorhanden sein, ermöglichen Sie die Auswahl zwischen 2 verschiedenen Charts (Line, Bar) welche über einen Button umgeschaltet werden können